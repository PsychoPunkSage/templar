\documentclass[11pt,a4paper]{article}

% ============================================
% PACKAGES
% ============================================
\usepackage[margin=0.5in]{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{fontawesome5}
\usepackage{tabularx}
\usepackage{multicol}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\usepackage{lmodern}
\usepackage{fix-cm}
\usepackage[hidelinks]{hyperref}

% ============================================
% CONFIGURATION - EDIT THESE TO CUSTOMIZE
% ============================================

% Color scheme
\definecolor{headercolor}{RGB}{0,51,102}
\definecolor{sectioncolor}{RGB}{0,51,102}
\definecolor{linkcolor}{RGB}{0,102,204}

% Spacing configuration
\newcommand{\sectionspacing}{1pt}
\newcommand{\itemspacing}{-2pt}
\newcommand{\beforeheaderskip}{-15pt}
% Server-injected spacing overrides (from LayoutPhysicsConfig via \renewcommand).
% Placed here so \renewcommand can override the defaults declared above.
{{LAYOUT_PARAMS}}

% Font sizes
\newcommand{\namefontsize}{\LARGE}
\newcommand{\sectionfontsize}{\large}
\newcommand{\normalfontsize}{\small}

% Section title formatting
\titleformat{\section}
  {\color{sectioncolor}\sectionfontsize\bfseries\uppercase}
  {}{0em}{}[\titlerule]
\titlespacing*{\section}{0pt}{\sectionspacing}{4pt}

% Remove page numbers
\pagestyle{empty}

% Hyperlink setup
\hypersetup{
    colorlinks=true,
    linkcolor=linkcolor,
    urlcolor=linkcolor,
    pdftitle={Abhinav Prakash - CV},
    pdfauthor={Abhinav Prakash}
}

% Custom commands
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% Item spacing
\setlist[itemize]{leftmargin=*, nosep, itemsep=\itemspacing, topsep=2pt}

% ============================================
% CUSTOM COMMANDS - FOR EASY CONTENT ENTRY
% ============================================

% Education entry: \education{Year}{Degree}{Institute}{CGPA}
\newcommand{\education}[4]{
  \textbf{#1} & \textbf{#2} & #3 & #4 \\
}

% Job entry: \job{Company}{Position}{Duration}
\newcommand{\job}[3]{
  \textbf{#1 | #2} \hfill \textbf{#3}
}

% Project entry: \project{Title}{Subtitle}{Duration}
% NOTE: #2 (tech_stack / subtitle) is accepted for signature compatibility but NOT rendered.
% The caller may pass an empty string or the tech stack; neither is shown in the heading.
\newcommand{\project}[3]{
  \textbf{#1} \hfill \textbf{#3}
}

% Competition entry: \competition{Name}{Achievement}
\newcommand{\competition}[2]{
  \textbf{#1 | #2}
}

% Skill category: \skillcat{Category}{Skills}
\newcommand{\skillcat}[2]{
  \textbf{#1:} #2
}

% ============================================
% DOCUMENT BEGINS
% ============================================

\begin{document}

\vspace*{\beforeheaderskip}

% ============================================
% HEADER — populated at render time
% ============================================
\begin{center}
    {\normalfontsize\textbf{\color{headercolor}{{FULL_NAME}}}} \\[2pt]
    {\footnotesize {{CONTACT_LINE}}}
\end{center}

\vspace{4pt}

% ============================================
% BODY — generated sections injected here
% ============================================
{{SECTIONS}}

\end{document}
